#define _CRT_SECURE_NO_DEPRECATE
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <graphics.h>
using namespace std;
/*
int flag_TIME = 0;

void Countdown()
{
	int i = 15, t;
	TCHAR now1[2];
	TCHAR now2[5];
	
	t = time(NULL);
	while (i >= 0)
	{
		outtextxy(10, 40, _T("µ√∑÷£∫"));
		outtextxy(10, 5, _T(" £”‡"));
		sprintf(now1, "%d", i);

		if (i < 10) outtextxy(75, 4, now1);
		else outtextxy(68, 4, now1);
		outtextxy(110, 5, _T("√Î"));

		sprintf(now2, "%d", score);
		outtextxy(100, 40, now2);

		if (t != time(NULL)) i--;
		t = time(NULL);
	}
	flag_TIME = 1;
}*/